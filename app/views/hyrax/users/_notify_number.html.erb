<%= link_to hyrax.notifications_path,
            'aria-label' => t("hyrax.toolbar.notifications"),
            class: "notify-number" do %>
    <span class="fa fa-bell"></span>
    <span class="count invisible label label-danger"><%= current_user.mailbox.inbox(unread: true).count %></span>
<% end %>
